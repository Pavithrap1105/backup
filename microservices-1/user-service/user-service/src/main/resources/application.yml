server:
  port: 9002

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bank?useSSL=false
    username: root
    password: root


  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  application:
    name: USER-SERVICE




